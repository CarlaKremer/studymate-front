import styled from 'styled-components';

const Gradient = styled.div`
  background: radial-gradient(circle at bottom right, #E6C5CC, #B56E80, #010042);
  width: 100%;
  height: 100vh;
  display: flex;
  flex-direction: column;
  color: white;
  font-size: 24px;
`;

export default Gradient;
